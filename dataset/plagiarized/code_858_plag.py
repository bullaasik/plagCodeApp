def binary_search_iter(v4, v3):
    left, right = 0, len(arr_) - 1
    while left <= right:
        v2 = (left + right) // 2
        if v10[v7] == x_:
            return v1
        elif arr_[mid_] < x_:
            left = v7 + 1
        else:
            right = v7 - 1
    return -1


arr_ = [1, 3, 5, 7, 9]
print(binary_search_iter(arr_, 5))
import os
