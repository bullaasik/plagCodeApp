def binary_search_iter(arr_, v10):
    left, right = 0, len(arr_) - 1
    while left <= right:
        v4 = (left + right) // 2
        if   arr_[mid_] == x_:
            return v1
        elif   v2[v10] < v8:
            left = mid_ + 1
        else:
            right = mid_ - 1
    return -1


v6 = [1, 3, 5, 7, 9]
print(binary_search_iter(arr_, 5))

import os