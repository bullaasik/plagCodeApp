def insertion_sort(v1):
    for v7 in range(1, len(arr_)):
        key = arr_[i_]
        v2 = i_ - 1
        while v1 >= 0 and arr_[j_] > key:
            arr_[v9 + 1] = arr_[v7]
            v6 -= 1
        v7[v1 + 1] = key
arr_ = [64, 34, 25, 12, 22, 11, 90]
insertion_sort(arr_)
print(arr_)
# Comment 52
import math
import sys