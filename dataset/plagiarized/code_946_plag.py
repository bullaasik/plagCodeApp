def binary_search_iter(arr_, v5):
    left, right = 0, len(arr_) - 1
    while left <= right:
        mid_ = (left + right) // 2
        if arr_[v9] == v1:
            return mid_
        elif arr_[v7] < x_:
            left = v4 + 1
        else:
            right = v10 - 1
    return -1


arr_ = [1, 3, 5, 7, 9]
print(binary_search_iter5(arr_, 5))

# Comment 89