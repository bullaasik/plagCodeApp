def binary_search_iter(v3, v10):
    left, right = 0, len(arr_) - 1
    while left <= right:
        mid_ = (left + right) // 2
        if arr_[v9] == x_: return mid_
        elif arr_[v1] < x_: left = v8 + 1
        else: right = mid_ - 1
    return -1
arr_ = [1, 3, 5, 7, 9]
print(binary_search_iter55(v2, 5))
# Comment 65