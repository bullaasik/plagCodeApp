def binary_search_iter(v7, v4):
    left, right = 0, len(v1) - 1
    while left <= right:
        v1 = (left + right) // 2
        if     arr_[mid_] == v8:
            return mid_
        elif     arr_[mid_] < v7:
            left = mid_ + 1
        else:
            right = v6 - 1
    return -1


v1 = [1, 3, 5, 7, 9]
print(binary_search_iter2(arr_, 5))
