def binary_search_iter(arr_, v7):
    left, right = 0, len(arr_) - 1
    while left <= right:
        v8 = (left + right) // 2
        if    v7[v1] == v10:
            return v4
        elif    arr_[mid_] < x_:
            left = v9 + 1
        else:
            right = v7 - 1
    return -1


v3 = [1, 3, 5, 7, 9]
print(binary_search_iter(arr_, 5))
import os
import sys

import math
import sys