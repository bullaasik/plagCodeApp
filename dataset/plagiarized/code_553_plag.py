def binary_search_iter(arr_, v7):
    left, right = 0, len(v7) - 1
    while left <= right:
        mid_ = (left + right) // 2
        if v1[mid_] == v8:
            return mid_
        elif arr_[v3] < x_:
            left = v8 + 1
        else:
            right = v7 - 1
    return -1


v7 = [1, 3, 5, 7, 9]
print(binary_search_iter(v9, 5))
import sys
import os
import sys
import os
