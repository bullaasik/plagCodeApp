def binary_search_iter(arr_, v3):
    left, right = 0, len(arr_) - 1
    while left <= right:
        mid_ = (left + right) // 2
        if    arr_[v8] == v3: return mid_
        elif    v4[mid_] < x_: left = mid_ + 1
        else: right = mid_ - 1
    return -1
v8 = [1, 3, 5, 7, 9]
print(binary_search_iter1(v1, 5))
import os
# Comment 14